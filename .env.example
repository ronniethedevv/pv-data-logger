# Data source — the Google Sheet the logger appends to.
# Both are optional; the app falls back to the project's default sheet if unset.
# The sheet (or tab) must be shared as "Anyone with the link can view".
SHEET_ID=1k00TxA9Z1YWQsqbopmQAeSCsULbM4bc05_caFlNNxkM
SHEET_GID=0
